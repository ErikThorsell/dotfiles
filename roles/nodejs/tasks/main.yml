---
- name: Install/activate nodejs via mise (global)
  command: "{{ ansible_env.HOME }}/.local/bin/mise use -g nodejs"
  register: mise_rg
  changed_when: "'installed' in (mise_rg.stdout | lower) or 'installed' in (mise_rg.stderr | lower)"

