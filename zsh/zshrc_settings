if [ -d "$HOME/.oh-my-zsh" ]; then
  export ZSH="$HOME/.oh-my-zsh"
  zstyle ':omz:update' mode reminder
  plugins=(git poetry)
  COMPLETION_WAITING_DOTS="true"
  HIST_STAMPS="yyyy-mm-dd"
  source $ZSH/oh-my-zsh.sh
fi

export EDITOR='vim'

echo " > .zshrc_settings loaded"
